<h1 tag="error" style="padding-top: 20px;">Looks like something bad happened</h1>


<center style="padding-top: 20px">
    
    <h3 id="issue">
        {{#if issue}}
            {{{issue}}}
        {{else}}
            There seems to be an issue with something you did.
        {{/if}}
        <br>If this issue persists, please contact your manager. </br>
    </h3>
    <h3 name="error">
    <br>
    {{#if redirectLink}}
    <a href="{{redirectLink}}"><b>{{{solution}}}</b></a>
    {{else}}
    <a href="/vendor/outstanding-orders">Tap here to return to your main orders page</a>
    {{/if}}
    </br>
    </h3>

</center>

